package com.example.interpreter.lexer

class Lexer {
    fun tokenize(source: String): List<Token> {
        TODO("Tokenize source into identifiers, literals, keywords, operators, separators, and EOF")
    }
}
